const validateAssignmentSchema = require('./validate-assignment-schema')
const validateSubmissionSchema = require('./validate-submission-schema')

module.exports = {
   validateAssignmentSchema,
   validateSubmissionSchema
}